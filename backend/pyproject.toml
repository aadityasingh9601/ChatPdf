[tool.fastapi]
entrypoint = "app.main:app"

[tool.vercel]
entrypoint = "app.main:app"

[tool.vercel.scripts]
build = "python build.py"

[project]
name = "chat-pdf"
version = "0.1.0"
requires-python = ">=3.12"